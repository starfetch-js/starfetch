# starfetch
Starfetch is an agent-ready TAP/ADQL toolkit for public astronomy table data. It provides a local MCP server, a scriptable CLI, and a distributable agent skill for inspecting TAP services and running bounded ADQL queries. A reusable TypeScript library powers those surfaces for callers that want direct API access.
